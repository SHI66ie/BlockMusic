[build]
  command = "pip install --upgrade pip && pip install -r requirements.txt"
  publish = "build"

[build.environment]
  PYTHON_VERSION = "3.9"
  NODE_VERSION = "18"
  PYTHON_VERBOSE = "true"

[context.production.environment]
  PYTHON_VERSION = "3.9"
